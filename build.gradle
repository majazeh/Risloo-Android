// NOTE: Top level build file where you can add configuration options common to all sub-projects modules.

buildscript {
    repositories {
        mavenCentral()
        google()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.2' // Android Build Tools
        classpath 'androidx.navigation:navigation-safe-args-gradle-plugin:2.3.5'// Navigation SafeArgs
        classpath 'com.google.gms:google-services:4.3.10' // Google Services
        classpath 'com.google.firebase:firebase-crashlytics-gradle:2.7.1' // Firebase Crashlytics

        // NOTE: Do not place your application dependencies here;
        // they belong in the individual module build.gradle files.
    }
}

allprojects {
    repositories {
        maven { url "https://maven.google.com" } // Google Url Maven
        maven { url "https://jitpack.io" } // JitPack Url Maven

        mavenCentral()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}